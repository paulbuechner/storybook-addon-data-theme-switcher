---
"storybook-addon-data-theme-switcher": minor
---

Toolbar button now reflects external `data-theme` attribute changes. If another script, addon, or DevTools modifies the `data-theme` attribute on the `<html>` element, the toolbar button automatically updates to show the current value.
