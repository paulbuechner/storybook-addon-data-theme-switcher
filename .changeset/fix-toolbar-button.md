---
"storybook-addon-data-theme-switcher": patch
---

fix: align toolbar button style with Storybook's built-in buttons

- Use `variant="ghost"` on `IconButton` to match Storybook's built-in toolbar button styling
